# staticcheck configuration
# https://staticcheck.dev/docs/configuration/
#
# SA1019: deprecation warnings.
#   We intentionally retain the `crypto/dsa` import for diagnostic-only
#   reporting of legacy DSA-keyed certificates (key-type display, not
#   verification). Migration / removal is tracked for the PR5 refactor.
checks = ["all", "-SA1019"]
